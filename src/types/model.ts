export enum ModelAuth {
  role = 'role',
  user = 'user',
  reg = 'reg',
}

export enum ModelProductSettings {
  category = 'category',
  subcategory = 'subcategory',
  group = 'group',
  fullTextSearchModel = 'fullTextSearch',
}

export enum ModelProducts {
  eav_attribute = 'eav_attribute',
  eav_entity = 'eav_entity',
  eav_value = 'eav_value',
  order = 'order',
}
